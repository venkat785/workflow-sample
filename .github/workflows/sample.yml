name: firstflow

on: push

jobs: 
  run-shell-command:
    runs-on: ubuntu-latest
    steps: 
      - name: echo a string
        run: 
          echo "Hello Workflow"
      - name: display versions
        run:
          node -v
          npm -v
          
